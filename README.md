# Image Colorizer AI

Приложение для раскрашивания черно-белых фотографий с использованием модели DeOldify и глубокого обучения.

## 📋 Содержание

- [Архитектура](#архитектура)
- [Быстрый старт](#быстрый-старт)
- [Установка](#установка)
- [Конфигурация](#конфигурация)
- [Использование](#использование)
- [API](#api)
- [Разработка](#разработка)
- [Развертывание](#развертывание)
- [Устранение неполадок](#устранение-неполадок)

## 🏗️ Архитектура

Проект построен на современном стеке с разделением на frontend и backend:

```
Image-Colorizer-AI/
├── backend/                    # FastAPI бэкенд
│   ├── app/                    # Основной Python пакет
│   │   ├── __init__.py
│   │   ├── main.py            # FastAPI приложение и API endpoints
│   │   ├── models.py          # SQLAlchemy модели базы данных
│   │   ├── database.py        # Конфигурация БД и сессии
│   │   ├── colorizer.py       # Интеграция с DeOldify
│   │   ├── config.py          # Конфигурация приложения
│   │   └── schemas.py         # Pydantic схемы для валидации
│   ├── storage/               # Хранилище файлов (опционально)
│   ├── tests/                 # Тесты
│   ├── requirements.txt       # Python зависимости
│   └── install_deoldify_fixed.sh  # Скрипт установки DeOldify
│
├── frontend/                   # React + TypeScript фронтенд
│   ├── src/
│   │   ├── App.tsx            # Главный компонент
│   │   ├── main.tsx           # Точка входа
│   │   ├── pages/             # Страницы приложения
│   │   │   ├── home.tsx       # Главная страница (загрузка)
│   │   │   ├── gallery.tsx   # Галерея изображений
│   │   │   ├── result.tsx    # Страница результата
│   │   │   └── public-result.tsx  # Публичный просмотр
│   │   ├── components/        # React компоненты
│   │   │   ├── layout.tsx    # Основной layout
│   │   │   ├── image-compare.tsx  # Сравнение до/после
│   │   │   ├── image-viewer.tsx   # Модальное окно просмотра
│   │   │   └── ui/            # UI компоненты (shadcn/ui)
│   │   └── hooks/             # React хуки
│   │       ├── use-images.ts  # Работа с API изображений
│   │       └── use-share.ts   # Функционал шаринга
│   ├── public/                # Статические файлы
│   └── index.html
│
├── ml/                        # ML модели и скрипты
│   ├── models/                # Веса предобученных моделей
│   │   └── ColorAize_weights.pth  # Модель DeOldify
│   ├── scripts/
│   │   └── download_model.py  # Скрипт загрузки модели
│   ├── dummy/                 # Dummy директория для DeOldify
│   └── data/                  # Датасеты (опционально)
│
├── deoldify_repo/            # Клонированный репозиторий DeOldify
│
├── start.sh                  # Скрипт запуска всего приложения
├── package.json              # Frontend зависимости
├── vite.config.ts            # Конфигурация Vite
└── README.md                 # Этот файл
```

### Технологический стек

**Backend:**
- FastAPI - современный веб-фреймворк
- SQLAlchemy - ORM для работы с БД
- DeOldify - модель для раскрашивания изображений
- PyTorch - фреймворк для ML
- SQLite/PostgreSQL - база данных

**Frontend:**
- React 18 + TypeScript
- Vite - сборщик
- TanStack Query - работа с API
- Tailwind CSS - стилизация
- shadcn/ui - UI компоненты
- Framer Motion - анимации
- Wouter - роутинг

## 🚀 Быстрый старт

### Автоматический запуск

```bash
# Установите зависимости и запустите все сервисы
./start.sh
```

Скрипт автоматически:
1. Проверит и установит Python зависимости
2. Установит DeOldify (если нужно)
3. Проверит наличие модели
4. Установит frontend зависимости
5. Запустит backend и frontend

### Ручной запуск

**1. Backend:**
```bash
cd backend
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
pip install -r requirements.txt

# Установка DeOldify
./install_deoldify_fixed.sh

# Запуск сервера
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**2. Frontend:**
```bash
npm install
npm run dev
```

**3. Загрузка модели (если еще не загружена):**
```bash
python ml/scripts/download_model.py ColorAize_weights
```

## 📦 Установка

### Требования

- Python 3.10+
- Node.js 18+
- npm или yarn
- ~2GB свободного места (для модели)

### Пошаговая установка

**1. Клонирование репозитория:**
```bash
git clone <repository-url>
cd Image-Colorizer-AI
```

**2. Backend:**
```bash
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
./install_deoldify_fixed.sh
```

**3. Frontend:**
```bash
npm install
```

**4. Загрузка модели:**
```bash
python ml/scripts/download_model.py ColorAize_weights
```

Модель будет загружена в `ml/models/ColorAize_weights.pth` (~1.5GB).

## ⚙️ Конфигурация

### Переменные окружения

Создайте файл `.env` в корне проекта (опционально):

```env
# Backend
DATABASE_URL=sqlite:///./backend/colorizer.db
DEOLDIFY_MODEL_PATH=./ml/models/ColorAize_weights.pth
PORT=8000
HOST=0.0.0.0

# Frontend
VITE_API_URL=http://localhost:8000
```

### Конфигурация модели

По умолчанию используется модель `ColorAize_weights.pth`. Чтобы использовать художественную модель:

1. Загрузите модель:
```bash
python ml/scripts/download_model.py ColorizeArtistic_gen
```

2. Установите переменную окружения:
```bash
export DEOLDIFY_MODEL_PATH=./ml/models/ColorizeArtistic_gen.pth
```

## 💻 Использование

### Загрузка изображения

1. Откройте приложение в браузере (обычно `http://localhost:5173`)
2. Перетащите черно-белое изображение в зону загрузки или нажмите для выбора файла
3. Дождитесь обработки (5-15 секунд)
4. Просмотрите результат

### Ограничения

- Максимальный размер файла: 10MB
- Поддерживаемые форматы: JPG, JPEG, PNG
- Рекомендуется использовать черно-белые или старые фотографии

### Функционал

- **Загрузка и обработка** - автоматическая колоризация изображений
- **Галерея** - просмотр всех обработанных изображений
- **Сравнение** - интерактивное сравнение оригинала и результата
- **Публичные ссылки** - безопасный шаринг результатов
- **Скачивание** - сохранение результатов в различных форматах

## 🔌 API

### Endpoints

**GET `/`** - Health check
```json
{
  "message": "Image Colorizer API",
  "status": "running"
}
```

**POST `/api/images`** - Загрузка изображения
- Content-Type: `multipart/form-data`
- Параметр: `file` (изображение)
- Возвращает: объект изображения с ID и статусом

**GET `/api/images`** - Список всех изображений
- Возвращает: массив изображений

**GET `/api/images/{id}`** - Получить изображение по ID
- Возвращает: объект изображения

**GET `/api/public/{token}`** - Публичный доступ по токену
- Возвращает: объект изображения (без ID)

### Формат ответа

```json
{
  "id": 1,
  "originalUrl": "data:image/jpeg;base64,...",
  "colorizedUrl": "data:image/jpeg;base64,...",
  "status": "completed",
  "errorMessage": null,
  "createdAt": "2024-01-01T12:00:00",
  "publicToken": "aB3dE5fG7hI9jK1lM3nO5pQ7rS9tU1vW3"
}
```

### Статусы

- `pending` - Ожидание обработки
- `processing` - Обработка
- `completed` - Завершено
- `failed` - Ошибка

## 🛠️ Разработка

### Структура кода

**Backend:**
- `app/main.py` - API endpoints и основная логика
- `app/colorizer.py` - Интеграция с DeOldify
- `app/models.py` - Модели базы данных
- `app/database.py` - Конфигурация БД

**Frontend:**
- `src/pages/` - Страницы приложения
- `src/components/` - Переиспользуемые компоненты
- `src/hooks/` - Кастомные React хуки

### Запуск в режиме разработки

Backend с автоперезагрузкой:
```bash
cd backend
source venv/bin/activate
uvicorn app.main:app --reload
```

Frontend с hot reload:
```bash
npm run dev
```

### Тестирование

```bash
# Backend тесты
cd backend
pytest

# Frontend тесты (если настроены)
npm test
```

## 🚢 Развертывание

### Production сборка

**Frontend:**
```bash
npm run build
# Результат в dist/
```

**Backend:**
```bash
cd backend
source venv/bin/activate
uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
```

### Docker (опционально)

```bash
docker-compose up -d
```

### Nginx конфигурация

Пример конфигурации в `nginx.conf` для production.

## 🐛 Устранение неполадок

### Модель не найдена

**Ошибка:** `DeOldify model not found`

**Решение:**
```bash
python ml/scripts/download_model.py ColorAize_weights
```

Проверьте, что файл существует:
```bash
ls -lh ml/models/ColorAize_weights.pth
```

### DeOldify не установлен

**Ошибка:** `No module named 'deoldify'`

**Решение:**
```bash
cd backend
source venv/bin/activate
./install_deoldify_fixed.sh
```

### Предупреждения fastai

Предупреждения о пустом training set - это нормально. Модель уже предобучена, обучение не требуется.

### Проблемы с загрузкой

- Проверьте размер файла (максимум 10MB)
- Проверьте формат (JPG, JPEG, PNG)
- Проверьте логи backend
- Проверьте консоль браузера (F12)

### Проблемы с SSL при загрузке модели

Если возникают проблемы с SSL при загрузке модели, скрипт автоматически использует unverified context для безопасных источников.

## 📝 Лицензия

MIT

## 🤝 Вклад

Приветствуются любые улучшения! Пожалуйста, создавайте issues и pull requests.

## 📞 Поддержка

При возникновении проблем:
1. Проверьте раздел "Устранение неполадок"
2. Проверьте логи backend и frontend
3. Создайте issue с описанием проблемы

## 📦 Git репозиторий

### Что НЕ коммитится в git:

- `node_modules/` - зависимости npm (устанавливаются через `npm install`)
- `venv/`, `.venv/` - виртуальное окружение Python (создается автоматически)
- `.local/` - локальные файлы системы
- `__pycache__/` - кэш Python
- `*.db` - базы данных (создаются автоматически)
- `dist/`, `build/` - собранные файлы
- `deoldify_repo/` - клонированный репозиторий DeOldify
- `*.log` - логи
- `.env` - переменные окружения (используйте `.env.example`)

### Первый коммит:

```bash
git add .
git commit -m "Initial commit: Image Colorizer AI"
```

Все необходимые исключения уже настроены в `.gitignore`.

---

**Примечание:** DeOldify требует значительных вычислительных ресурсов. Для лучшей производительности рекомендуется использовать GPU.
